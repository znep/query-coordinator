// A placeholder test, not sure PublishedStory will even exist.
describe('PublishedStory class', function() {
  describe('constructor', function() {
    it('should accept zero arguments', function() {
      assert.isObject(new PublishedStory());
    });
  });
});
