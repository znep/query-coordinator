<div class="admin-breadcrumb">
  <h1>
    <%- if content_for?(:breadcrumb) %>
      <%= admin_nav_link_to('screens.admin.index.page_title', '/admin') %>
      <span class="socrata-icon-arrow-right separator"></span>
      <span class="current-page"><%= yield :breadcrumb %></span>
    <%- else %>
      <%= t('screens.admin.index.page_title') %>
    <%- end %>
    <%- if current_user.try(:is_superadmin?) -%>
      <a class="internal-link" href="<%= url_for :controller => '/internal', :action => 'index' %>">
        <i class="socrata-icon-logo"></i> <span><%= t('screens.admin.index.internal') %></span>
      </a>
    <%- end %>
  </h1>
</div>
