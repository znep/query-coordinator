<% content_for :head do %>
  <%= rendered_stylesheet_tag 'administration-screen' %>
  <%= rendered_stylesheet_tag 'administration-metadata' %>
  <title>Manage Metadata | <%= CurrentDomain.strings.site_title %></title>
<% end %>

<% content_for :skip_links do %>
  <a href="?no_js=true">Disable Javascript on this page</a>
<% end %>

<%= render :partial => 'left_nav' %>

<div class="contentBox withLeftNavigation metadataScreen">
   <div class="adminHeader metadata">
    <span class="icon"></span>
    <h1>Manage Dataset Metadata</h1>
  </div>

  <%= display_standard_flashes %>

  <div class="customFields">
    <h2>Define Custom Fields</h2>
    <%- if CurrentDomain.custom_dataset_metadata.blank? -%>
      <div class="noData">
        No custom dataset fields have been defined for your domain yet.
        Enter a name below and click 'Create' to begin.
      </div>
    <%- else -%>
      <%- @metadata.each_with_index do |fieldset, fs_index| -%>
        <a href="<%= url_for :action => 'delete_metadata_fieldset',
          :fieldset => fs_index %>" class="button remove removeFieldsetButton">
          <span class="icon"></span>
          Remove
        </a>
        <h3 class="fieldsetTitle"><%= fieldset['name'] %></h3>
        <div class="gridListWrapper">
          <%= form_tag({:action => 'create_metadata_field', :fieldset => fs_index},
            {:class => 'createMetadataFieldForm'}) %>
            <table class="gridList metadataFieldTable" cellspacing="0">
              <colgroup>
                <col class="required" />
                <col class="name" />
                <col class="actions" />
              </colgroup>
              <thead>
                <tr>
                  <th class="required"><div>&nbsp;</div></th>
                  <th class="name"><div>Field Name</div><span class="icon"></span></th>
                  <th class="actions"><div>&nbsp;</div><span class="icon"></span></th>
                </tr>
              </thead>
              <tbody>
                <%- if fieldset['fields'].present? -%>
                  <%- fieldset['fields'].each_with_index do |field, index| -%>
                    <%- required = field['required'].present? -%>
                    <tr class="item">
                      <td class="required<%= ' requiredEnabled' if required %>">
                          <span class="requiredField"></span>
                      </td>
                      <td class="name">
                        <span class="cellInner">
                          <%= field.name %>
                        </span>
                      </td>
                      <td class="actions">
                        <span class="loading"></span>
                        <%= link_to raw('<span class="icon"></span>Remove'),
                            { :action => 'delete_metadata_field',
                              :fieldset => fs_index, :index => index },
                              {:class => 'button removeField remove'} %>
                        <%= link_to 'Make ' + (required ? 'Optional' : 'Required'),
                              { :action => 'toggle_metadata_required',
                                :fieldset => fs_index, :index => index },
                              {:class => 'button toggleRequired'} %>
                        <%= link_to raw('<span class="icon"></span>Up'),
                              { :action => 'move_metadata_field',
                                :fieldset => fs_index, :field => field.name,
                                :direction => 'up' },
                              { :class => 'button moveButton upButton upArrowNoHover' +
                                ((index == 0) ? ' disabled' : '') } %>
                        <%= link_to raw('<span class="icon"></span>Down'),
                              { :action => 'move_metadata_field',
                                :fieldset => fs_index, :field => field.name,
                                :direction => 'down' },
                              { :class => 'button moveButton downButton downArrowNoHover' +
                                ((index == fieldset['fields'].length-1) ? ' disabled' : '') } %>
                      </td>
                    </tr>
                  <%- end -%>
                <%- else -%>
                  <tr>
                    <td colspan="3">
                      <div class="flash notice">
                        This field set does not have any fields defined yet
                      </div>
                    </td>
                  </tr>
                <%- end -%>
                <tr>
                  <td class="required">
                  </td>
                  <td class="name">
                    <input type="text" class="newFieldName" name="newFieldName" />
                  </td>
                  <td class="actions">
                    <input class="button newFieldButton" type="submit" value="Add New Field" />
                  </td>
                </tr>
              </tbody>
            </table>
          </form>
        </div>
      <%- end -%>
    <%- end -%>
    <div class="newFieldsetArea">
      <h2>Create a New Field Set</h2>
      <%= form_tag({ :action => 'create_metadata_fieldset' }, { :class => 'newFieldsetForm' }) %>
        <label>Name</label>
        <input type="text" name="newFieldsetName" class="newFieldsetName" />
        <input class="button newFieldsetButton" type="submit" value="Create" />
      </form>
    </div>
  </div>

  <div class="categories">
    <h2>Define Dataset Categories</h2>

    <div class="gridListWrapper">
      <%= form_tag({:action => 'create_category'},
        {:class => 'createCategoryForm'}) %>
        <table class="gridList categoriesTable" cellspacing="0">
          <colgroup>
            <col class="name" />
            <col class="actions" />
          </colgroup>
          <thead>
            <tr>
              <th class="name"><div>Category Name</div><span class="icon"></span></th>
              <th class="actions"><div>&nbsp;</div><span class="icon"></span></th>
            </tr>
          </thead>
          <tbody>
            <%- @categories.each do |category, value| -%>
              <tr class="item">
                <td class="name">
                  <span class="cellInner">
                    <%= category.titleize_if_necessary %>
                  </span>
                </td>
                <td class="actions">
                  <%= link_to raw('<span class="icon"></span>Remove'),
                      { :action => 'delete_category',
                        :category => category },
                        {:class => 'button removeField remove'} %>
                </td>
              </tr>
            <%- end -%>
            <tr>
              <td class="name">
                <input type="text" class="newCategory" name="new_category" />
              </td>
              <td class="actions">
                <input class="button newCategoryButton" type="submit" value="Add New Category" />
              </td>
            </tr>
          </tbody>
        </table>
      </form>
  </div>

</div>

<% content_for :js_footer do %>
  <%- unless params[:no_js] == 'true' -%>
    <%= include_javascripts 'admin-metadata' %>
  <%- end -%>
<% end %>
