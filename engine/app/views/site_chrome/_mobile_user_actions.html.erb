<div class="mobile-user-actions">
  <% if logged_in? %>
    <%= link_to('/profile', :class => 'profile-link mobile-btn') do %>
      <%= content_tag(:span, nil, :class => 'icon-user')  %>
      <%= username %>
    <% end %>
  <% else %>
    <%= link_to('Sign In', '/login', :class => 'mobile-btn') %>
  <% end %>
</div>
