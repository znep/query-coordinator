<%= form_object.hidden_field :inviteToken %>
<%= form_object.hidden_field :authToken %>
<%= form_object.hidden_field :firstName %>
<%= form_object.hidden_field :lastName %>
<%= hidden_field_tag 'signup[accept_terms]', 'true' %>

<%- required_span = "<span class=\"accessible\">#{t('core.forms.required_field')}</span>" -%>

<div class="line">
  <div class="labelLine">
    <%= form_object.label :email, "#{t('account.common.form.email')} #{required_span}".html_safe,
      :class => 'required main' %>
  </div>
  <%= form_object.text_field :email %>
</div>

<div class="line">
  <div class="labelLine">
    <%= form_object.label :screenName, "#{t('account.common.form.display_name')} #{required_span}".html_safe,
      :class => 'required main' %>
  </div>
  <%= form_object.text_field :screenName, :maxlength => 40 %>
</div>

<%- unless local_assigns.fetch(:hide_password, false) -%>
<div class="line">
  <div class="labelLine">
    <%= form_object.label :password, "#{t('account.common.form.password')} <span class=\"accessible\">#{t('account.common.form.password_requirements')}</span>".html_safe,
      :class => 'required passwordLabel main' %>
    <span class="passwordHint about">(<%= t('account.common.form.password_restrictions') %> <span class="icon">&nbsp;</span>)</span>
  </div>
  <%= form_object.password_field :password %>
</div>

<div class="line clearfix">
  <div class="labelLine">
    <%= form_object.label :passwordConfirm, "#{t('account.common.form.confirm_password')} #{required_span}".html_safe,
      :class => 'required main' %>
  </div>
  <%= form_object.password_field :passwordConfirm %>
</div>
<%- end -%>

<p class="line">
  <%= render 'accounts/recaptcha2' %>
</p>

<p class="signUpHint">
  <%= t('screens.sign_up.terms_html') %>
</p>

<div class="formActionButtons">
  <input type="submit" class="button default" value="<%= t('screens.sign_up.form.create_account_button') %>" />
  <span class="accountSignupHint">
    <%= link_to t('screens.sign_up.form.have_account_link'), login_path, :class => 'signInLink' %>.
  </span>
</div>
