# This environment config is loaded by bin/migration_aws_env.
#
# This is not meant to run a rails server, so we don't have any configurations
# set in here.

Rails.application.configure do
  config.eager_load = false
end
