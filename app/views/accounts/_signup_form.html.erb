<% form_for :signup, @signup, :url => account_path, :html => {:id => 'signupForm', :multipart => true} do |f| %>
 <%= render :partial => "accounts/signup_form_data", :locals => { :f => f, :is_modal => is_modal } %>
<% end %>

