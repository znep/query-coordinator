<!doctype html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html;charset=utf-8" />
    <%= rendered_stylesheet_tag 'bower-styles-all' %>
    <%= yield :head %>
  </head>
  <body ng-app="<%= params['app']%>">
    <%= render params['app'] %>
    <%= yield :layout %>
  </body>
</html>
