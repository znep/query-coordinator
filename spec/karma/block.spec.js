// A placeholder test, not sure Block will even exist.
describe('Block class', function() {
  describe('constructor', function() {
    it('should accept zero arguments', function() {
      assert.isObject(new Block());
    });
  });
});
