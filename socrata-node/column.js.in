(function() {
    var _ = require('underscore');
    var $ = require(__dirname + '/blist-util');
    var blist = require(__dirname + '/blist-compat');

    var ServerModel = require(__dirname + '/server-model');
    var ColumnContainer = function()
    {
        require(__dirname + '/column-container').call(arguments);
    };

    var isDataset = function(obj)
    {
        return obj instanceof require(__dirname + '/dataset');
    };

#include "column.src.js"
})();
