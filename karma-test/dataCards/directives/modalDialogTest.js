describe('modalDialog', function() {

  xit('should close the modal dialog and not add a card when the "x" button is clicked', function() {

    expect(false).to.be.true;
  });

  xit('should close the modal dialog and not add a card when the area outside the dialog is clicked', function() {

    expect(false).to.be.true;
  });

});
