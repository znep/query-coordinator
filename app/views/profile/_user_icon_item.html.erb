<%- contact = user_icon_item -%>

<li>
  <a href="<%= profile_path(contact.id) %>", title="<%= h(contact.displayName) %>">
    <img src="<%= contact.profile_image_path("small") %>" width="24" height="24"
    alt="<%= h(contact.displayName) %>" />
  </a>
</li>
