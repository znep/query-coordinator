import React from 'react';
import { connect } from 'react-redux';

export const App = () => {
  return (
    <div>
      TODO! add stuff
    </div>
  );
};

export default connect((state) => (state))(App);
