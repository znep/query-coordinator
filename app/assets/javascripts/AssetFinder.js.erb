;var AssetFinder = (function() {

  function AssetFinder() {

    this.getRelativeUrlRoot = function() {
      return '<%= config.relative_url_root %>';
    };

    this.getStyleAssetPath = function(name) {

      var assetPaths = {
        'rich-text-editor-iframe': '<%= asset_path("rich-text-editor-iframe/rich-text-editor-iframe.css") %>'
      }

      return assetPaths[name];
    };
  }

  return AssetFinder;
})();
