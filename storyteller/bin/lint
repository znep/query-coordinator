#!/bin/bash

npm update eslint > /dev/null

NEW_PROBLEMS=`./node_modules/.bin/eslint -c package.json -f compact app spec/karma | sort | tail -n +3`

if [[ ! -z "$NEW_PROBLEMS" ]]; then
  echo
  echo "New errors:"
  echo "$NEW_PROBLEMS"
  echo
  false
else
  true
fi
